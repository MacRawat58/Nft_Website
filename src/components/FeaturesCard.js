import React from "react";
import {Container } from "reactstrap"
const FeaturesCard = () =>{
    return (<div>

            <Container className="featureCard">
                <h2 className="feature--Cardheader">Set up your wallet</h2>
                <p className="feature--Cardp">Once you’ve set up your wallet of choice, connect it to OpenSea.
Learn about the wallets we support.</p>
            </Container>
    </div>
    )
}

export default FeaturesCard;